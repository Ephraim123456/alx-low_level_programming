#include <string.h>
/**
 * _memcpy - copies memory area
 *@dest: Destination string
 *@src: source char
 *@n: integer value
 *Return: memcpy
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	cpy = memcpy(*dest, *src, n);
	return (cpy());
}
