#include <stdio.h>

/**
 * main - function to print its name
 * @argc: counts the number argument on the command line
 * @argv: prints the string argument
 * Return: Always return 0 (sucess)
 */
int main(int argc, char *argv[])
{

	printf("%s\n", argv[0]);

	return (0);
}
