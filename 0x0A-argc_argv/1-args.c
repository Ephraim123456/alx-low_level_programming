#include <stdio.h>
/**
 * main - Entry point
 * @argc: argument counts
 * @argv: argument vector
 * Return: Always 0 (Success)
 */
int main(int argc, *char argv[])
{
	(void)argv:
	printf("%d", argc);
	return (0);
}
